import styled from '@emotion/styled';

export const Input = styled.input`
  display: inline-block;
  width: 100%;
  font: inherit;
  font-size: 20px;
  border: none;
  outline: none;

  margin-bottom: 24px;
  padding: 8px 4px;

  &:placeholder {
    font: inherit;
    font-size: 18px;
  }
`;
